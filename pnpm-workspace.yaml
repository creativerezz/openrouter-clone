# pnpm 11 reads project settings from here (no longer from package.json "pnpm").
# We don't use next/image, so sharp's native build is unnecessary — explicitly
# declining it stops pnpm from treating it as an "ignored build" and exiting 1.
allowBuilds:
  sharp: false
